<h2><%= @series.title %></h2>

<%= link_to 'Edit series', edit_series_path(@series) %><br>
<%= link_to 'Delete series', series_path(@series),
            method: :delete,
            data: { confirm: 'Are you sure you want to delete this series?' }
%><br>

<%= image_tag @series.image.url(:medium) %><br>

<%= link_to 'Back', series_index_path %>