<h2><%= @sermon.title %></h2>

<%= link_to 'Edit sermon', edit_sermon_path(@sermon) %>

<p>
  <strong>Description: </strong>
  <%= @sermon.description %>
</p>

<p>
  <%= image_tag @sermon.image.url(:medium) %>
</p>

<p>
  <strong>Sermon date: </strong>
  <%= @sermon.date %>
</p>

<%= link_to 'Delete sermon', sermon_path(@sermon),
            method: :delete,
            data: { confirm: 'Are you sure you want to delete this sermon?' }%>

<br>

<%= link_to 'Back', sermons_path %>